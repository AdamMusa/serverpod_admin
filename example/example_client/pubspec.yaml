name: example_client
description: Starting point for a Serverpod client.

environment:
  sdk: '^3.8.0'

dependencies:
  serverpod_auth_idp_client: ^3.4.10
  serverpod_admin_client: ^1.0.8
  serverpod_client: ^3.4.10
